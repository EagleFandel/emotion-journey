# Web
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PostgreSQL (required for production; app falls back to in-memory when empty)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/emotion_journey

# Comma-separated emails that can access /dashboard
ADMIN_USERS=admin@admin.local