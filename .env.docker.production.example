# App
APP_PORT=3000
NEXT_PUBLIC_APP_URL=https://emotion.example.com
ADMIN_USERS=founder@example.com,ops@example.com

# PostgreSQL
# Keep POSTGRES_PORT for host debugging only. In production,
# expose DB to localhost or private network only.
POSTGRES_PORT=5432
POSTGRES_DB=emotion_journey
POSTGRES_USER=emotion
POSTGRES_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_PASSWORD

